# ARLQ, another rogue-like quest game

ARLQ（Another Rogue-Like Quest）は、人間とChatGPTが共同で作成した実験的なローグライクゲームです。Pygameとcursesに対応したPython実装です。

* コードは、ゲームのおおまかな内容の記述をもとにChatGPTで生成し、その後人間が細部を調整・改善しました。
  * ChatGPTは、コード生成以外にも迷路生成アルゴリズムの作成やcursesライブラリの使い方の解説を担当しました。
  * タイプヒントやコメントもChatGPTによって生成されています。
  * 新しいモンスターのアイデアにもChatGPTが協力しています。
* マニュアルは、人間がドラフトを作成し、後にChatGPTと共に加筆・校正を行いました。

![](screenshot.png)

## インストールと起動

以下のコマンドでインストールします。

```bash
pipx install git+https://github.com/tos-kamiya/arlq
```

インストール後、`arlq` と `arlq-cli` を実行できます。

```bash
arlq --stage 1
arlq --stage 3
arlq-cli --stage 1
```

主なオプション:

- `-T` / `--large-torch`: 視界を広げる
- `-t` / `--small-torch`: 視界を狭める
- `-n` / `--narrower-corridors`: 通路を狭くする
- `--stage 1`、`--stage 2`、`--stage 3`: ステージを直接選択する
- `--debug-show-entities`: 未発見のエンティティも表示する
- `--seed VALUE`: 整数のシードまたはバージョン付きシード文字列を指定する
- `--rematch`: 前回のステージとシードを再利用する（同じ配置を再現するには他のレイアウト関連オプションも同じにする。`--seed`・`--stage`との併用不可）
- `--curses`: cursesを使用する
- `--version`: バージョンを表示する

ゲーム開始時にステージとシードをユーザーのキャッシュディレクトリ内の
`arlq/last-seed` に保存します。`--rematch`だけで前回のステージを直接開始できます。
`--seed`・`--stage`を併用した場合や、保存値がない場合・壊れている場合はエラーになります。

`--curses`を利用する場合は、必要に応じてcursesパッケージを別途インストールしてください。

Ubuntu 24.04の場合:

```bash
sudo apt install python3-curses
```

Windowsの場合:

```bash
pip install windows-curses
```

## ゲームの説明

### 目的

毎回異なる通路を持つダンジョンを探索し、竜が隠した宝箱を見つけます。ステージ1では竜の宝箱、ステージ2では火竜の宝箱を探します。

ステージ3は、3フロアにわたって `W`（Dread Wyrm）を討伐する遠征です。Isolated Elfは情報を伝え、Javelin Elfはプレイヤーに同行して攻撃を支援し、Collector Elfは呪われた剣と引き換えに恒久的な武器強化を与えます。エルフの信頼を得ると、High Elfからワームのバリアを通過するためのタリスマンを受け取れます。`W`を倒した後、3階の解錠された `T` に接触するとクリアです。

通常プレイでは、`I`、`J`、`K`、`H` がそれぞれ1体ずつ、シードから決まるフロアに配置されます。`K` は1階または2階に配置され、3階には`A`が2体、`c`と`C`は配置されません。フロアを下りる階段は `v`、上る階段は `^` で表示されます。`o` はすべてのフロアに1体ずつ配置されます。`m`、`X`、`e`、`g` は種類ごとにゲーム開始時の乱数で1フロアだけが選ばれ、他のフロアには配置されません。

### プレイヤーとフォグ

`@`を矢印キーまたはWASDで操作します。移動するたびにLP（Life Points）が減少し、LPが0になると飢えでゲームオーバーになります。歩いた場所を中心に視界が表示され、一度訪れた場所はオートマップに残ります。

### モンスターと同行者

未遭遇のモンスターは `?`、同行者は `!` と表示されます。接触すると種類が判明し、同じ種類のエンティティはその文字で表示されます。通常のモンスターや一時的な同行者は自分では移動しませんが、Javelin Elfはプレイヤーの移動前の位置へ追従します。

モンスターに接触すると戦闘になります。自分の攻撃力以下の敵を倒すとレベルアップし、持ち物や食料を取得できます。強いモンスターに敗北するとチェックポイントへ戻されます。`a`、`A`、`b`、`c`、`C` は倒しても再出現しません。同行者は一定時間、固有の能力を提供します。

竜または火竜を倒すと宝箱の封印が解除され、解錠された `T` に接触するとクリアです。剣は最大3回まで壁を破壊でき、毒状態では攻撃力が低下します。

## モンスター一覧

### ステージ1

| 表示 | 名前 | 説明 |
| --- | --- | --- |
| `a` | アメーバ | 最初から倒せる弱い敵。レベルアップに役立つ。 |
| `A` | レアアメーバ | 大幅にレベルアップできる。 |
| `b` | バイソン | 倒すとLPを大きく回復する。 |
| `c` | キメラ | 剣を持ち、攻撃力が1.5倍になる。 |
| `d` | コモドドラゴン | 強力でLPを回復するが、毒を残す。 |
| `D` | 竜 | 竜の宝箱を解錠する。 |

### ステージ2

ステージ1の竜以外のモンスター・同行者に加えて、次の敵が登場します。

| 表示 | 名前 | 説明 |
| --- | --- | --- |
| `C` | レアキメラ | 呪われた剣を持ち、攻撃力を3倍にするがLPを消費する。 |
| `e` | エレボス | 戦闘時にLPを少し奪う。 |
| `F` | 火竜 | 火竜の宝箱を解錠する。 |
| `g` | ゴーレム | 倒すと岩が飛び散り、食料は得られない。 |
| `H` | ハイエルフ | エルフの功績を得るまで、あなたを認めない。 |
| `X` | まきびし植物 | 周囲にまきびしを撒く。 |

### ステージ3

ステージ2の火竜以外のモンスター・同行者に加えて、次の敵とエルフが登場します。

| 表示 | 名前 | 説明 |
| --- | --- | --- |
| `l` | ループ | 接触すると世界を80ターン巻き戻す。巻き戻し後も敵の種類だけは忘れない。 |
| `m` | マイコニド | 別のモンスターに接触するまで視界を狭める。 |
| `w` | ウィルム | バリアに守られている。 |
| `W` | ドレッドウィルム | バリアに守られており、討伐すると宝物庫が開く。 |
| `I` | 隠遁エルフ | 他のエルフに関する情報を伝える。壁で閉じた部屋にいる。 |
| `J` | ジャベリンエルフ | プレイヤーに同行し、攻撃力を25%高める。 |
| `K` | コレクターエルフ | 呪われた剣と引き換えに、恒久的な攻撃力1.2倍の強化を与える。壁は壊せない。 |
| `H` | ハイエルフ | `I`、`J`、`K` のうち2名以上と知り合うとタリスマンを授ける。 |

## 同行者一覧

| 表示 | 名前 | 能力 |
| --- | --- | --- |
| `n` | ノミコン | 隣接したモンスターの種類を判別する。 |
| `o` | オキュラー | 視界を少し広げる。 |
| `p` | ペガサス | 壁を越えて移動できる。 |

## 開発用コマンド

```bash
uv run -p .venv/bin/python python -m compileall src
uv run -p .venv/bin/python python -c "import arlq"
uv run -p .venv/bin/python pytest
uv run -p .venv/bin/python ruff check src
```

## ライセンス

本プロジェクトはBSD-2-Clauseライセンスのもとで公開されています。

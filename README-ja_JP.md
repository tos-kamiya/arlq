# ARLQ, another rogue-like quest game

ARLQ（Another Rogue-Like Quest）は、人間とAIsが共同で作成した実験的なローグライクゲームです。PygletとBlessed端末UIに対応したPython実装です。

<p align="center">
  <a href="https://pypi.org/project/arlq/">
    <img alt="PyPI version" src="https://img.shields.io/pypi/v/arlq" />
  </a>
  <a href="https://www.python.org/">
    <img alt="Python 3.10-3.14" src="https://img.shields.io/badge/Python-3.10--3.14-3776AB?logo=python&logoColor=white" />
  </a>
</p>

![](screenshot.png)

## インストールと起動

Python 3.10以降が必要です。リリース版は以下のコマンドでインストールできます。

```bash
pipx install arlq
```

GitHub上の最新版を直接インストールする場合は、以下を実行してください。

```bash
pipx install git+https://github.com/tos-kamiya/arlq
```

必要なPygletとBlessedも自動的にインストールされます。
インストール後、`arlq` と `arlq-cli` を実行できます。

```bash
arlq --stage 1
arlq --stage 3
arlq-cli --stage 1
```

端末UIを使用する場合は、端末の大きさを80桁×24行以上にしてください。
端末をそれより小さくするとゲームは一時停止し、十分な大きさに戻すと再開します。

主なオプション:

- `-T` / `--large-torch`: 視界を広げる
- `-t` / `--small-torch`: 視界を狭める
- `-n` / `--narrower-corridors`: 通路を狭くする
- `--stage 1`、`--stage 2`、`--stage 3`: ステージを直接選択する
- `--debug-show-entities`: 未発見のエンティティも表示する
- `--seed VALUE`: 整数のシードまたはバージョン付きシード文字列を指定する
- `--rematch`: 前回のステージとシードを再利用する（同じ配置を再現するには他のレイアウト関連オプションも同じにする。`--seed`・`--stage`との併用不可）
- `--terminal`: Blessedを使用した端末UIを使用する
- `--curses`: `--terminal`の互換用 alias（非推奨）
- `--version`: バージョンを表示する
- `--lang en` / `--lang ja`: ゲーム内メッセージとステージ選択画面の言語を指定する。`--lang auto`（既定）はロケールから自動判定する。ステータスバーの略語やアイテム名は言語に関わらず英語表示のまま。

ゲーム開始時にステージとシードをユーザーのキャッシュディレクトリ内の
`arlq/last-seed` に保存します。`--rematch`だけで前回のステージを直接開始できます。
`--seed`・`--stage`を併用した場合や、保存値がない場合・壊れている場合はエラーになります。

端末UIに必要なBlessedは、ARLQのインストール時に自動的にインストールされます。

## ゲームの説明

### 目的

毎回異なる通路を持つダンジョンを探索し、竜が隠した宝箱を見つけます。ステージ1では竜の宝箱、ステージ2では火竜の宝箱を探します。

ステージ3では、国王からの依頼であるドレッドウィルムの討伐と、その宝箱の取得の2つが目標です。エルフたちの助けを借りると攻略に役立ちます。

ステージ3は複数のフロアからなり、フロア間の移動は、下りる階段 `v` と上る階段`^` で行います。4人のエルフ`I`、`J`、`K`、`H`が登場します。

### プレイヤーとフォグ

`@`を矢印キーまたはWASDで操作します。移動するたびにLP（Life Points）が減少し、LPが0になると飢えでゲームオーバーになります。歩いた場所を中心に視界が表示され、一度訪れた場所はオートマップに残ります。

### モンスターと同行者

未遭遇のモンスターは `?`、同行者は `!` と表示されます。接触すると種類が判明し、同じ種類のエンティティはその文字で表示されます。通常のモンスターや一時的な同行者は自分では移動しませんが、Javelin Elfはプレイヤーの移動前の位置へ追従します。

モンスターの種類の判別はフロアをまたいで保持されます。一方、ステージ3の同行者の種類はフロアごとに判別されるため、新しいフロアでは未遭遇の同行者が再び `!` と表示されます。

モンスターに接触すると戦闘になります。自分の攻撃力以下の敵を倒すとレベルアップし、持ち物や食料を取得できます。強いモンスターに敗北するとチェックポイントへ戻されます。ただし、他のモンスターと接触することなく同じモンスターに連続で2回敗北した場合は、マップ上のランダムな位置へ逃げ出します（通路を塞ぐ倒せないモンスターによってゲームが詰んでしまうことを防止します）。

`a`、`A`、`b`、`c`、`C` は倒しても再出現しません。同行者は一定時間、固有の能力を提供します。

竜または火竜を倒すと宝箱の封印が解除され、解錠された `T` に接触するとクリアです。剣は最大3回まで壁を破壊でき、毒状態では攻撃力が通常の1/2になります。

### レアタイプと強化個体

一部のモンスターには、通常個体とは異なる特徴を持つレアな個体が存在します。例えば `A`（レアアメーバ）は大幅にレベルアップでき、`C`（レアキメラ）は攻撃力を大きく高める呪われた剣を与えてくれます。いずれもプレイヤーにとって嬉しい特別な効果です。

一方、ステージ2以降には強化個体も登場します。強化個体はレアタイプとは異なり、プレイヤーにとっての恩恵ではなく、単に通常の3倍のレベルを持つ強い敵というだけです。画面上では本体の右隣にアポストロフィを付けた `b'` のように表示され、通常個体とは別の種類として判別されます。

## モンスター一覧

### ステージ1

| 表示 | 名前 | 説明 |
| --- | --- | --- |
| `a` | アメーバ | 最初から倒せる弱い敵。レベルアップに役立つ。 |
| `A` | レアアメーバ | 大幅にレベルアップできる。 |
| `b` | バイソン | 倒すとLPを大きく回復する。 |
| `c` | キメラ | 剣を持ち、攻撃力が1.5倍になる。 |
| `d` | コモドドラゴン | 強力でLPを回復するが、毒を残す。 |
| `D` | 竜 | 竜の宝箱を解錠する。 |

### ステージ2

ステージ1の竜以外のモンスター・同行者に加えて、次の敵が登場します。

ステージ2には通常の `b` バイソンが3体、強化個体の `b` が3体登場します（強化個体については上記「レアタイプと強化個体」を参照）。

| 表示 | 名前 | 説明 |
| --- | --- | --- |
| `C` | レアキメラ | 呪われた剣を持ち、攻撃力を3倍にするがLPを消費する。 |
| `e` | エレボス | 戦闘時にLPを少し奪う。 |
| `F` | 火竜 | 火竜の宝箱を解錠する。 |
| `g` | ゴーレム | 倒すと岩が飛び散り、食料は得られない。 |
| `H` | ハイエルフ | 強すぎて倒せない。 |
| `X` | まきびし植物 | 周囲にまきびしを撒く。 |

### ステージ3

ステージ3は3つのつながったフロアで構成されます。エルフたちの助けを借りると、ドレッドウィルムの討伐と宝箱の取得に役立ちます。
ステージ3には、`b`、`c`、`d` の強化個体である `b'`、`c'`、`d'` が登場します。

#### モンスター

| 表示 | 名前 | 説明 |
| --- | --- | --- |
| `m` | マイコニド | 別のモンスターに接触するまで視界を狭める。 |
| `w` | ウィルム | バリアに守られている。 |
| `W` | ドレッドウィルム | バリアに守られており、討伐すると宝物庫が開く。 |

#### エルフ

| 表示 | 名前 | 説明 |
| --- | --- | --- |
| `I` | 隠者エルフ | 他のエルフに関する情報を伝える。壁で閉じた部屋におり、壁を壊せる剣が必要。2回目以降の接触では、閉じ込められないようマップ上のランダムな位置へ送り返される。 |
| `J` | ジャベリンエルフ | プレイヤーに同行し、攻撃力を25%高める。 |
| `K` | コレクターエルフ | 呪われた剣と引き換えに、恒久的な攻撃力1.2倍の強化を与える。壁は壊せない。 |
| `H` | ハイエルフ | `I`、`J`、`K` のうち2名以上と知り合うとタリスマンを授ける。 |

## 同行者一覧

| 表示 | 名前 | 能力 |
| --- | --- | --- |
| `l` | ループ同行者 | ステージ3にのみ登場する。接触すると世界を80ターン巻き戻す。巻き戻し後も敵の種類だけは忘れない。 |
| `n` | ノミコン | 視界内にいるすべてのモンスターの種類を判別する。 |
| `o` | オキュラー | 視界を大幅に広げる。 |
| `p` | ペガサス | ステージ2以降に登場する。壁を越えて移動できる。 |

## 開発用コマンド

```bash
uv run -p .venv/bin/python python -m compileall src
uv run -p .venv/bin/python python -c "import arlq"
uv run -p .venv/bin/python pytest
uv run -p .venv/bin/python ruff check src
```

## ライセンス

本プロジェクトはBSD-2-Clauseライセンスのもとで公開されています。
